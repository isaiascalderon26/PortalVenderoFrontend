/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentsclienteRemunerativoComponent } from './cliente-remunerativo.component';

describe('ComponentsclienteRemunerativoComponent', () => {
  let component: ComponentsclienteRemunerativoComponent;
  let fixture: ComponentFixture<ComponentsclienteRemunerativoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ComponentsclienteRemunerativoComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ComponentsclienteRemunerativoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
